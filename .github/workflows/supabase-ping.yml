name: Supabase Ping

on:
  schedule:
    - cron: "0 */6 * * *"
  workflow_dispatch:

jobs:
  ping:
    name: Keep Supabase project active
    runs-on: ubuntu-latest

    steps:
      - name: Ping Supabase project
        run: |
          curl -sS --fail "${{ secrets.SUPABASE_URL }}/auth/v1/health"